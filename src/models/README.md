# Models Directory

This directory should contain data schemas and definitions (e.g., Mongoose schemas/models or Sequelize models if using SQL).

## Best Practices
- Define your data schema and DB representation here.
- Keep model-specific hooks, validations, and instance methods here.
- Do NOT place complex business logic here; that belongs in the `services/` layer.
